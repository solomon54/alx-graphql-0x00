query CombinedCharacterData {
  # 2. Fetch single character details (using the singular field)
  singleCharacter: character(id: 2) {
    id
    name
    status
    species
    gender
  }

  # 2. Fetch the paginated list (using the plural field)
  characterPage: characters(page: 2) {
    info {
      count
      pages
      next
      prev
    }
    results {
      id
      name
    }
  }
}